{
  "name": "whatsapp-api-monorepo",
  "version": "1.0.0",
  "description": "Run frontend and backend with a single command",
  "scripts": {
    "start": "concurrently \"npm start --prefix backend\" \"npm start --prefix frontend\"",
    "postinstall": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
